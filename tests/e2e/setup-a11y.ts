/**
 * Accessibility Testing Setup
 * NOTE: vitest-axe appears to have limited API. Using axe-core directly for now.
 * We'll rely on @axe-core/playwright for E2E tests instead.
 */

import { expect } from 'vitest';

// Placeholder for future vitest-axe integration
// For now, we'll use @axe-core/playwright in E2E tests

export {}; // Make this a module
